apiVersion: v1
kind: Service
metadata:
  name: agent-runtime
  namespace: brick-breaker
  labels:
    app.kubernetes.io/name: agent-runtime
    app.kubernetes.io/part-of: project-sentinel
    app.kubernetes.io/managed-by: flux
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-runtime
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
